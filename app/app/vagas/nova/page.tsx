import type { Metadata } from "next"; import JobWizardClient from "@/app/components/jobs/JobWizardClient"; import { activeOrganization,getAdminContext } from "@/app/components/admin/admin-data"; export const metadata:Metadata={title:"Nova vaga | JobForged"}; export default async function Page(){const context=await getAdminContext(activeOrganization.id);return context?<JobWizardClient context={context}/>:null}
