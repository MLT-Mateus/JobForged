import type { Metadata } from "next"; import JobsListClient from "@/app/components/jobs/JobsListClient"; import { activeOrganization,getAdminContext } from "@/app/components/admin/admin-data"; export const metadata:Metadata={title:"Vagas | JobForged"}; export default async function Page(){const context=await getAdminContext(activeOrganization.id);return context?<JobsListClient context={context}/>:null}
